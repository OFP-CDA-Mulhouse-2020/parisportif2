{% extends 'base.html.twig' %}

{% block title %}Email de changement de mot de passe envoyé{% endblock %}

{% block body %}
    {# HEADER #}
    {% include "partials/_header_band_logo.html.twig" %}

    <div class="container">
        <div class="row d-flex justify-content-center">
            <div class="col-sm-9">
                <h1 class="h3 py-5">Changement du mot de passe</h1>
                <p>Un email a été envoyé avec un lien pour changer votre mot de passe. Ce lien expirera dans {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }} heure(s).</p>
            </div>
        </div>
    </div>
{% endblock %}         <p>Si vous n'avez pas reçu d'email, veuillez vérifier vos spams ou <a href="{{ path('app_forgot_password_request') }}">envoyer de nouveau</a>.</p>

